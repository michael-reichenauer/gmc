package tools

import (
	"testing"
)

func TestGet(t *testing.T) {
	// releasesUri := "https://api.github.com/repos/michael-reichenauer/gmc/releases"
	//
	// b, err := httpGet(releasesUri)
	// assert.NoError(t, err)
	// t.Log(string(b))

}
